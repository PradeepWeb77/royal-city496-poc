# Copy to .env and fill in for testnet deployment. NEVER commit the real .env.

# RPC endpoint for the Sepolia testnet (e.g. Alchemy / Infura URL)
SEPOLIA_RPC_URL=

# Private key of the deploying account (testnet only — keep it funded with test ETH).
# Do NOT use a key that holds real funds.
PRIVATE_KEY=
